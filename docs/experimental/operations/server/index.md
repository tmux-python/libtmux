# Server operations

Server operations address the tmux server as a whole: lifecycle, buffers,
shell commands, configuration files, and hierarchy-wide listings.

```{tmuxop:catalog}
:scope: server
:toctree:
```
